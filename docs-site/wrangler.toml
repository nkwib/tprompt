# Cloudflare Workers + Static Assets configuration for the tprompt docs site.
# Deployed via `wrangler deploy` after `pnpm build` produces ./build.

name = "tprompt"
compatibility_date = "2026-05-04"

[assets]
directory = "./build"
not_found_handling = "404-page"
